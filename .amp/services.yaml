services:
  preview:
    command: python3 -u -m http.server "$PORT" --bind 0.0.0.0
    health: /
    portal:
      title: Knowhere landing
      description: Live static preview with no dependency install or build step.
    portals:
      - folder: Alternate prototypes
        links:
          - url: /landing-source.html
            title: Landing source
          - url: /document-scan-section.html
            title: Document scan section
